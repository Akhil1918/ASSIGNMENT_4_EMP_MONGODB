<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Database</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/form.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="/server/data">
                <i class="fas fa-users me-2"></i>Employee Database
            </a>
            <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <% for (let i=0; i < nav.length; i++) { %>
                        <li class="nav-item">
                            <a class="nav-link active text-white" href="<%=nav[i].link %>">
                                <%= nav[i].name %>
                            </a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>


    <!-- Update Form -->
    <div class="container d-flex justify-content-center align-items-center">
        <div class="form-container">
            <form method="POST" action="/server/update/<%= employeeid %>" class="employee-form">
                <h2 class="form-title mb-4">
                    <i class="fas fa-user-edit me-2"></i>Update Employee
                </h2>
                
                <div class="mb-4">
                    <label for="EmployeeName" class="form-label">
                        <i class="fas fa-user me-2"></i>Name
                    </label>
                    <input type="text" class="form-control" name="EmployeeName" value="<%= employee.EmployeeName %>" id="EmployeeName">
                </div>
                
                <div class="mb-4">
                    <label for="EmployeeDesignation" class="form-label">
                        <i class="fas fa-briefcase me-2"></i>Designation
                    </label>
                    <input type="text" class="form-control" name="EmployeeDesignation" value="<%= employee.EmployeeDesignation %>" id="EmployeeDesignation">
                </div>
                
                <div class="mb-4">
                    <label for="EmployeeLocation" class="form-label">
                        <i class="fas fa-map-marker-alt me-2"></i>Location
                    </label>
                    <input type="text" class="form-control" name="EmployeeLocation" value="<%= employee.EmployeeLocation %>" id="EmployeeLocation">
                </div>
                
                <div class="mb-4">
                    <label for="EmployeeSalary" class="form-label">
                        <i class="fas fa-dollar-sign me-2"></i>Salary
                    </label>
                    <input type="number" class="form-control" name="EmployeeSalary" value="<%= employee.EmployeeSalary %>" id="EmployeeSalary">
                </div>
                
                <button type="submit" class="btn btn-primary submit-btn">
                    <i class="fas fa-save me-2"></i>Update
                </button>
            </form>
        </div>
    </div>
</body>
</html>
